{#
UI Element template
type: articles_selection
element fields :
    title
    display
    articles
    buttonLabel
    buttonUrl
element methods:
    getArticles
#}

{% set articles = ui_element.getArticles(element) %}

{{ include('@MonsieurBizSyliusBlogPlugin/Shop/UiElement/articles_cards.html.twig') }}
